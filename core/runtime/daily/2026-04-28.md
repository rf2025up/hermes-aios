# 2026-04-28 日志

## 凌晨（4/27晚延续）

### 飞书配对问题深度排查
- **根因**：OpenClaw有两套allowFrom存储
  1. `openclaw.json` 里的 channels.feishu.accounts.*.allowFrom
  2. `~/.openclaw/credentials/feishu-*-allowFrom.json` 独立文件
- 之前只改了openclaw.json，没改credentials目录，导致配对反复失败
- 修复：补全credentials目录下所有allowFrom文件
- Gateway热重载不刷新credentials缓存，需要完整restart才生效

### 贾维斯Agent状态反复
- 禁用agent用disabled字段 → config被reject → Gateway自动恢复backup
- backup里没有ops/kitchen → 两个agent注册信息丢失
- 最终恢复ops（贾维斯），按老板要求**移除kitchen（杨姐小助手）**
- 杨姐小助手：agent + binding + 飞书account全部删除，workspace文件保留

### 财经视频转写md
- `H:\临时视频转文案处理\财经类相关信息\transcripts` 下9个docx全部转md
- spawn子agent完成，0失败

### 贾维斯批量视频转音频任务
- **01:21 cron**派给贾维斯：`H:\抖音直播录制\` 下5个文件夹
- 5个文件夹：云、凌霄清华学习方法_59384364579、托管老司机_AmazingGrace02、抖音、朋阳近期直播25年10月
- 每个文件夹对应新建一个`_音频`文件夹存放mp3
- 完成后结果发飞书

### 记账
- 托管买菜63元，食材，营业账户（已在4/27日志记录）

### 待办
- 财经3000字报告需求（上下文压缩丢失，老板提过但具体要求不明，素材已就位）
- 贾维斯配对问题（贾维斯在处理主bot配对）
- 千问浏览器登录（待扫码）
- 清理openclaw-weixin重复插件

### 🔴 教训
1. **改config不要随便加字段**：disabled字段不是合法的agent配置，导致整个config被reject
2. **allowFrom有两套存储**：openclaw.json + credentials/ 目录，改一个不够
3. **Gateway恢复backup会丢东西**：backup可能是旧版本，新增的agent/配置会丢失
4. **不要擅作主张禁用飞书**：老板说禁后台动作，不是禁飞书
